# Core data / ML -- must match the versions the saved joblib pipelines
# were trained with (xgboost is pinned: 3.x changed serialization in a
# way that breaks SHAP's TreeExplainer against these saved models).
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
xgboost==2.1.4
imbalanced-learn==0.14.2

# Explainability
shap==0.49.1

# Plotting (SHAP waterfall chart rendered server-side to PNG)
matplotlib==3.10.9

# Model persistence
joblib==1.5.3

# Web app
flask==3.1.0
