# Leftover debug scratch file from diagnosing a module-naming collision
# (see pipeline.py). Not used anywhere in the pipeline. Kept only because
# files in the shared outputs folder cannot be deleted.
